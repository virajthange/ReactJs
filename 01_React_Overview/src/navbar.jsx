function Navbar() {
  return <h1>This is Navbar Component</h1>;
}
export default Navbar;

// It doesn't work if we put the component name start with lower case
// It is not mandatory that we have to match the component name with .jsx file name
// We can start file name with lower or upper case - no problem but standart wasy is upper case